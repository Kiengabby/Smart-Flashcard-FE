import { Routes } from '@angular/router';
import { FlashcardStudyComponent } from './flashcard-study.component';

export const FLASHCARD_STUDY_ROUTES: Routes = [
  {
    path: '',
    component: FlashcardStudyComponent
  }
];

